function shout(string) {
  return string.toUpperCase();
}

function whisper(string) {
  return string.toLowerCase();
}

function logShout(string) {
  var shout = string.toUpperCase;
  
  if(shout === string.toUpperCase) {
    console.log('You did it!');
  }
}